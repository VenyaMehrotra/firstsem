#include <stdio.h>

int main() {
    char date[11];
    printf("Enter date (dd/04/yyyy): ");
    gets(date);

    // Print day
    printf("%.2s-", date);

    // Print month as "Apr"
    printf("Apr-");

    // Print year
    printf("%.4s\n", date + 6);

    return 0;
}
