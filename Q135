#include <stdio.h>

enum Numbers {
    ONE = 10,
    TWO = 11,
    THREE = 12,
    FOUR = 13
};

int main() {
    printf("Value of ONE = %d\n", ONE);
    printf("Value of TWO = %d\n", TWO);
    printf("Value of THREE = %d\n", THREE);
    printf("Value of FOUR = %d\n", FOUR);
    
    return 0;
}
